  <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span> <span class="icon-bar"></span> <span class="icon-bar"></span> <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand hidden-sm" href="{{menu.homeRoute}}">{{menu.name}}</a>
      </div>

      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          {{#each menu.items}} 
            {{#if subItems}}
            <li class="dropdown{{#if selected}} active{{/if}}"><a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">{{title}} <span class="caret"/></a>
              <ul class="dropdown-menu" role="menu">
                {{#each subItems}}
                <li {{#if selected}} class="active"{{/if}}><a href="{{prefix}}{{route}}">{{title}}</a></li> 
                {{/each}}
              </ul></li>
            {{else}}
            <li {{#if selected}} class="active"{{/if}}><a href="{{prefix}}{{route}}">{{title}}</a></li> 
            {{/if}} 
          {{/each}}
        </ul>
        <ul class="nav navbar-nav navbar-right">
          {{#each menu.rightItems}}
          <li {{#if selected}} class="active"{{/if}}><a href="{{prefix}}{{route}}">{{title}}</a></li> 
          {{/each}}
        </ul>
      </div>
    </div>
  </nav>