const myMoney = 20

if ( myMoney > 10 ) {
  let taxHaven = myMoney * myMoney
  
  const constObject = {name: "Keith"}
  constObject.gutter = true
  console.log( constObject )

  taxHaven = taxHaven + 100
  console.log( taxHaven )
}
